# PLAN.md

> PRD.md 기반 개발 계획. `/pdca plan` 스킬의 취지(목표·성공기준·의존순서 정리)를 따르되, PRD.md에 이미 "개발 단위"로 합의된 순서를 그대로 formal Plan으로 정리했다.

## 이번 사이클 목표

PRD.md에 정의된 Must 기능(평가 시점 자동 알림 및 기수별 링크 생성, AI 대화 및 응답 자동 정리)을 실제로 동작하는 Next.js 앱으로 구현해, 신규간호사의 사직 위험을 3/6/9개월·1년 시점에 자동으로 감지하고 행정간호사가 통계로 확인할 수 있게 한다.

## 성공 기준

1. 행정간호사가 로그인해서 명단·과거 데이터를 엑셀로 업로드할 수 있다.
2. 입사일 기준으로 기수가 자동으로 묶이고, 3/6/9개월·1년 경과 시점에 맞는 링크가 자동 생성된다.
3. 퇴사 표시된 인원은 이후 알림 대상에서 제외된다.
4. 신규간호사가 링크로 접속해 Flight Risk 체크 → 마찰 지도 → Stay Menu(또는 Stay Point) 대화를 기기 기반 식별로 완료할 수 있다.
5. 완전 익명 / 부서만 공개 / 이름까지 공개 선택이 대화 시작 전 한 번 정상 동작하고, 완전 익명·부서만 공개로 시작한 3/6/9개월 대상자는 대화 끝에서 한 번 더 공개 전환 여부를 확인하며(1년 경과 Stay Point 대상자는 다시 묻지 않는다), 전환하면 그 대화에서 남긴 응답도 함께 전환된다.
6. 위급 신호 감지 시 동의 기반 알림(자기공개 동의 → 앱 내 알림)이 규칙대로 동작한다.
7. 응답이 자동으로 통계화되어 행정간호사 전용 대시보드(로그인 보호)에서 확인된다.
8. `npm run lint`, `npm run build`가 오류 없이 통과한다.

## 작업 순서 (먼저 되면 나머지가 쉬워지는 순)

1. 데이터 모델 설계 — 명단(사번/이름/부서/입사일/전화번호/퇴사여부), 응답, 기수 구조 정하기
2. 행정간호사 로그인 및 비밀번호 재설정(이메일) 기능 구현
3. 명단·과거 데이터 엑셀 업로드 기능 구현
4. 입사일 기준 기수 묶기 + 3/6/9개월·1년 경과 시점 자동 계산 로직 구현
5. 퇴사자 표시 시 이후 알림 대상에서 제외하는 로직 구현
6. 기수별 대화 링크(URL) 자동 생성 기능 구현
7. 링크 접속 시 기기(브라우저) 기반 응답자 식별 로직 구현
8. Flight Risk 체크 대화 흐름 구현
9. 마찰 지도 질문 흐름 구현
10. Stay Menu 질문 흐름 구현 (3/6/9개월 대상)
11. Stay Point 질문 흐름 구현 (1년 경과 대상)
12. 공개 수준 선택(완전 익명 / 부서만 공개 / 이름까지 공개) UI 및 저장 로직 구현 — 대화 시작 전 한 번만 고르고, 완전 익명·부서만 공개로 시작한 3/6/9개월 대상자에게는 대화 끝에서 한 번만 공개 전환 여부를 물어봄(1년 경과 Stay Point 대상자는 다시 묻지 않음)
13. 위급 신호 감지 및 자기공개 동의 흐름 구현
14. 응답 자동 저장 및 통계 집계 로직 구현
15. 행정간호사용 통계 대시보드 화면 구현 (개선 조치 기록 입력 포함)
16. 위급 신호 알림(앱 내 알림) 연동 구현
17. 신규간호사 대상 통계 공유 카드 구현 — 마찰 지도 답변 직후엔 마찰 지도 통계, Stay Menu 답변 직후엔 같은 시점 선배들의 이야기, 대화 종료 시엔 행정 개선 조치+선배 한마디+응원의 한마디를 단계별로 노출 (완전 익명·부서만 공개로 시작한 3/6/9개월 대상자에게는 대화 끝에서 공개 전환 의향을 한 번만 확인하는 단계 포함, 1년 경과 Stay Point 대상자는 제외)
18. 선배 한마디 카드 / 참여 인정 메시지 기능 구현
19. 재확인 체크인 로직 구현 (다음 시점 대화 시작 시, 새 질문 전에 이전 마찰 항목을 먼저 물어봄 — 여러 시점에 걸쳐 항목이 여러 개 쌓였다면 최신 것 하나만이 아니라 전부 누적으로 하나씩 차례로 물어봄)
20. Vercel 배포

## 배포 후 보완 작업

첫 배포(20번) 이후 실제 사용 중 나온 요청·버그를 반영한 기록. 번호는 붙이지 않고 시간 순으로 정리한다.

- 재확인 체크인(19번)이 최신 마찰 항목 하나만 물어보던 것을, 그동안 쌓인 항목 전부를 하나씩 차례로 물어보도록 변경 (`get_friction_map_history`로 누적 조회, `RecheckStep`이 항목마다 순서대로 진행)
- 위급 신호 알림(16번) 목록에서 각 항목을 펼치면, 그 사람이 같은 대화(같은 기수+기기)에서 남긴 답변 전체("답안지")를 바로 확인할 수 있는 기능 추가 — 행정간호사가 알림 옆에서 맥락을 따로 찾아볼 필요 없게 함
- 대화 끝 "도움을 받고 싶은지" 확인(17번) 흐름에, 신원을 밝힌 뒤 "실제로 이야기를 나눠보고 싶어요" / "간호국에 마음만 전달해주세요" 2개 선택지를 추가 — 선택 결과는 위급 신호 알림에 문구로 남아 행정간호사가 접촉 방식(직접 연락 vs 전달만)을 구분해서 대응할 수 있게 함. 이 서비스는 실시간 1:1 상담 기능이 없으므로(PRD 6번 비범위) "대화"를 골라도 앱 안에서 바로 연결되지 않고, 행정간호사가 알림을 보고 별도로 연락하는 방식이다
- 위 흐름에서, 이미 "이름까지 공개"를 고르고 시작한 사람은 신원 확인 절차(이름 입력)만 생략하고 "도움을 받고 싶은지" 질문 자체는 동일하게 받도록 수정 — 원래는 이름까지 공개한 사람에게는 이 질문 단계 자체가 통째로 생략되어 도움을 요청할 방법이 없었던 버그였음
