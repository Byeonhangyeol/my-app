# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## 프로젝트

이 저장소는 [PRD.md](PRD.md)에 정의된 서비스 — 행정간호사 1인이 신규간호사의 사직 위험을 조기에 감지·대응하는 도구 — 를 만든다. **PRD.md가 요구사항의 근거 문서**이며, 기능을 구현하기 전에 항상 먼저 읽는다. 용어(Flight Risk 체크, 마찰 지도, Stay Menu, Stay Point), must/nice 기능 구분(5번), 범위·비범위(6번), 개발 순서("개발 단위" 섹션)가 모두 PRD.md에 정의되어 있다. 현재 코드는 손대지 않은 `create-next-app` 기본 스캐폴드 상태다.

## 명령어

- `npm run dev` — 개발 서버 실행 (http://localhost:3000)
- `npm run build` — 프로덕션 빌드
- `npm run start` — 빌드된 결과 실행 (`build` 먼저 필요)
- `npm run lint` — ESLint 실행 (flat config, `eslint.config.mjs`)

아직 테스트 러너는 설정되어 있지 않다.

## 아키텍처

- Next.js App Router (`src/app`), TypeScript, React 19.
- 스타일은 Tailwind CSS v4 (`@tailwindcss/postcss`) — v4는 CSS-first 방식이라 별도 `tailwind.config`가 필요 없고, `src/app/globals.css`에서 직접 설정한다.
- 경로 별칭 `@/*` → `src/*` (`tsconfig.json`).
- `src/app/layout.tsx`가 `next/font/google`로 Geist Sans/Mono 폰트를 불러와 CSS 변수(`--font-geist-sans`, `--font-geist-mono`)로 노출한다.

## PRD.md에서 정해진 아키텍처 제약

아래는 이미 확정된 제품 결정이며, 구현하며 임의로 바꿀 수 있는 부분이 아니다 — 위반하면 기획서와 어긋난다.

- **기술 스택은 Next.js로 고정**(PRD 8번), 배포는 Vercel. `.env`에 `SUPABASE_ACCESS_TOKEN`, `OPENAI_API_KEY`가 있는 것으로 보아 데이터/인증은 Supabase, 대화형 AI는 OpenAI를 쓸 것으로 보이나, 확실치 않으면 사용자에게 먼저 확인한다.
- **인증되는 사용자는 행정간호사 한 명뿐**(로그인 + 이메일 비밀번호 재설정, PRD 7번). 신규간호사는 로그인하지 않는다 — 계정, 비밀번호, 사번/이름 입력 화면 자체가 없다.
- **식별은 개인이 아니라 기기 단위**: 신규간호사는 브라우저에 남는 식별자로만 인식되고, 로그인으로 식별되지 않는다. 같은 입사월로 묶인 "기수" 하나당 링크 하나만 발급한다 — 개인별 링크를 만들지 않는다(행정간호사의 대량문자 발송 기능이 동일 내용 일괄 발송만 지원하기 때문).
- **이 서비스는 문자를 직접 발송하지 않는다.** 기수 링크와 발송 시점만 행정간호사에게 알려주고, 실제 발송과 수신자 선정은 행정간호사가 본인의 기존 대량문자 도구로 직접 한다(PRD 4번).
- **행정간호사 기준 기본값은 익명이다.** 신규간호사는 대화를 시작하기 전 완전 익명 / 부서만 공개 / 이름까지 공개 중 한 번만 고른다 — 답변마다 다시 묻지 않는다. 완전 익명·부서만 공개로 시작한 3/6/9개월 대상자는 대화 맨 끝에서 "도움을 받고 싶은지"를 물으면서 그때 한 번만 공개 전환 의향을 확인하고, 전환하면 그 대화(같은 기수)에서 남긴 응답도 함께 공개된다. 1년 경과(Stay Point) 대상자는 공개 수준을 다시 묻지 않는다.
- **위급 신호 대응은 동의 기반이며, 임의로 신원을 밝히지 않는다.** 자·타해 위급 신호가 감지되면 AI가 먼저 "행정간호사에게 알려도 될지" 동의를 구하고, 동의한 경우에만 신원과 함께 전달한다. 무응답·거절 시에는 익명으로만 전달한다. 위급 신호 판단 자체도 고정 키워드 목록이 아니라 AI의 맥락 판단으로 한다.
- **없는 데이터를 지어내지 않는다** — 예: 실제 장소 추천 데이터가 없으므로 공방·맛집 등 장소 추천 기능을 임의로 만들지 않는다(PRD 6번 비범위).
- **신규간호사에게 다시 공유할 수 있는 내용은 PRD 7번의 화이트리스트로 제한**된다(마찰 지도 결과, 1년 이상 Stay Point, 같은 기수의 통계, 행정 개선 조치, 선배 한마디/참여 인정 콘텐츠). 그 외 내용은 행정간호사만 볼 수 있다.
- **명시된 비범위(PRD 6번)**: 파트장·프리셉터·교육파트 관여 기능, 실시간 1:1 상담 기능, 다기관 지원, 사내 인사시스템 연동, 네이티브 모바일 앱 — 이런 기능은 요청 없이 추가하지 않는다.

## Next.js 버전 참고

이 저장소는 사전 릴리스 버전인 Next.js `16.3.3`을 쓰고 있어, 기존에 학습된 정보와 API·컨벤션이 다를 수 있다. `AGENTS.md`에 안내된 대로, 프레임워크 관련 코드를 작성하기 전에 `node_modules/next/dist/docs/`의 문서를 먼저 확인한다.

## 작업 규칙

- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다.
- 기술 스택은 PRD에 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 Vercel을 사용한다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

## 작업 절차 (검증 루프)

코드를 바꿀 때는 아래 순서를 통과할 때까지 반복한다.

1. **변경한다** — 계획한 코드 수정을 적용한다.
2. **정적 검증** — `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다.
3. **화면 확인** — 화면이 바뀌는 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다.
4. **문제가 있으면 고치고 1)로 돌아간다.**
5. **통과하면** 무엇을 왜 바꿨는지 한 줄로 요약해 알린다.
