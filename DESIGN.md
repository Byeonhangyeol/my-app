# DESIGN.md

> PLAN.md·PRD.md 기반 설계. `/pdca design` 스킬의 취지(화면·데이터 흐름·기술 선택 정리)를 따르되, 비개발자도 읽을 수 있는 가벼운 문서로 정리했다.

## 1. 화면 구성

### 행정간호사용 (로그인 필요)

| 화면 | 무엇이 보이는지 |
|---|---|
| 로그인 | 아이디/비밀번호 입력, "비밀번호를 잊으셨나요" 링크(이메일 재설정). 별도 가입 화면 없이 행정간호사 본인의 계정 하나만 미리 만들어둔다 |
| 대시보드 홈 | 지금 발송해야 할 기수(예: "6월신규 3개월차") 배너, 최근 위급 신호 알림 |
| 명단 관리 | 엑셀 업로드 버튼, 명단 표(이름/전화번호/입사일/퇴사 여부 토글) |
| 기수·발송 관리 | 기수별 카드(3개월/6개월/9개월/1년경과 Stay Point), 상태(발송 전/발송됨), 링크 복사 버튼 |
| 통계 대시보드 | 마찰 지도 비율, Stay Point 비율, 위급 신호 발생 현황, 신규간호사에게 공유될 화면 미리보기, **개선 조치 기록 입력**(행정간호사가 직접 텍스트로 작성 — 화이트리스트 4번 항목용, 지금은 내용 없음), **위급 신호 알림 항목마다 "답안지 보기"로 그 사람이 같은 대화에서 남긴 답변 전체를 펼쳐볼 수 있음** |

### 신규간호사용 (로그인 없음, 링크로 접속)

| 화면 | 무엇이 보이는지 |
|---|---|
| 대화 화면 (3/6/9개월) | 채팅형 UI — **(재확인 체크인) 이전 시점에 답했던 마찰 항목이 있으면 맨 처음에 "저번에 말씀하신 그거, 요즘은 어때요?"를 먼저 물어봄 — 여러 시점에 걸쳐 항목이 여러 개 쌓였으면 최신 것 하나만이 아니라 전부 하나씩 차례로 물어봄** → **대화를 시작하기 전 이 의견들을 어떻게 전달할지 한 번 선택(완전 익명/부서만/이름까지)** → Flight Risk 체크 → 마찰 지도 답변(**이 단계에서 위급 신호를 감지 — 감지되면 "행정간호사에게 알려도 될까요?" 동의 팝업**) → ① 마찰 지도 통계 카드 노출 → Stay Menu 답변(객관식, "기타"만 직접 입력) → ② 같은 시점 선배들의 Stay Menu 이야기 카드 노출 → **대화 끝에서 "도움을 받고 싶은지" 확인 — 완전 익명·부서만 공개로 시작했다면 그때 한 번만 공개 전환(신원 확인) 의향을 묻고, 이미 이름까지 공개했다면 신원 확인 절차만 생략한 채 이 질문은 동일하게 받는다. "도움을 받고 싶어요"를 고르면(신원 확인 후) "실제로 이야기를 나눠보고 싶어요" / "간호국에 마음만 전달해주세요" 중 하나를 한 번 더 선택하고, 그 선택은 위급 신호 알림에 문구로 남는다** → ③ 행정 개선 조치 + 선배 한마디 + **응원의 한마디(참여해준 것 자체에 대한 감사 메시지)** 노출 → 종료 |
| 대화 화면 (1년 경과, Stay Point) | 같은 채팅형 UI로 Stay Point 질문 두 개(본인이 버틴 이유 → 후배에게 필요한 것)만 물어보는 별도 트랙 (Stay Menu와는 별개) → 공개 수준은 대화 시작 전 고른 값을 그대로 쓰고 다시 묻지 않음 → Stay Point 통계 공유 카드 → **1년 근속 축하 선물상자 카드(감사 문구를 무작위로 하나 보여줌)** → 행정 개선 조치 + 선배 한마디 + 응원의 한마디 → 종료 |
| 공유 통계 카드 | 대화 중 끝에 한 번에 몰아서 보여주지 않고, 답변 직후마다 순차적으로 노출한다: 마찰 지도 답변 직후엔 마찰 지도 통계만, Stay Menu 답변 직후엔 같은 시점 선배들의 이야기만, 대화 맨 마지막엔 행정 개선 조치·선배 한마디·응원의 한마디만 — 그때그때 관련된 것만 보여준다. 공개 여부는 대화 시작 전 한 번만 고르고 답변마다 다시 묻지 않는다 — 3/6/9개월 대상자는(1년 경과 Stay Point 대상자는 제외) 대화 맨 끝에서 "도움을 받고 싶은지"를 공통으로 확인한다: 완전 익명·부서만 공개로 시작했다면 그때 공개 전환(신원 확인) 의향을 묻고 전환에 동의하면 그 대화에서 남긴 응답도 함께 공개로 바뀌며, 이미 이름까지 공개했다면 신원 확인 절차만 생략한다. 어느 쪽이든 도움을 원하면 "실제 대화"와 "마음 전달만" 중 원하는 방식을 한 번 더 선택한다 |

## 2. 데이터 흐름 (입력 → 처리 → 출력)

1. **명단 등록**: 엑셀 업로드 → 표 내용을 읽어 이름/전화번호/입사일로 변환 → 명단 데이터로 저장 (과거 데이터도 행정간호사가 같은 형식으로 정리해 같은 방식으로 업로드)
2. **시점 계산**: 행정간호사가 로그인할 때마다 저장된 입사일 기준으로 기수를 묶고 3/6/9개월·1년 경과(Stay Point) 시점을 계산 → 시점이 된 기수가 있으면 그 자리에서 알리고 그 기수용 링크를 생성 (퇴사 표시된 인원은 이 계산에서 제외)
3. **대화 진행**: 신규간호사가 링크 클릭 → 브라우저에 식별 표시 생성/확인(이전 방문 기록 있으면 재확인 체크인으로 먼저 인사 — 여러 시점에 걸쳐 쌓인 마찰 항목이 있으면 전부 하나씩 차례로 물어봄) → 대화 시작 전 공개 수준(완전 익명/부서만/이름까지) 한 번 선택 → Flight Risk 체크 → 마찰 지도 답변 → Stay Menu(또는 Stay Point) 답변 → 답변마다 그때 고른 기본 공개 수준 태그를 붙여 저장 → 3/6/9개월 대상자는 대화 끝에서 "도움을 받고 싶은지"를 공통으로 확인하고(1년 경과 Stay Point 대상자는 제외), 완전 익명·부서만 공개로 시작했다면 그때 공개 전환 의향도 함께 확인해 전환하면 그 대화에서 남긴 응답도 함께 갱신 — 이미 이름까지 공개했다면 신원 확인 절차만 생략한다. 도움을 원하면 "실제 대화"/"마음 전달만" 중 방식도 한 번 더 선택한다
4. **위급 처리**: 답변 중 위급 신호를 AI가 판단 → 동의를 물어봄 → 동의하면 신원과 함께, 아니면 익명으로 앱 내 알림으로 행정간호사에게 전달 (문자는 보내지 않음) → 행정간호사는 알림에서 그 사람이 남긴 답변 전체("답안지")를 펼쳐 맥락을 바로 확인할 수 있다
5. **통계·공유(단계별)**: 쌓인 답변을 자동으로 집계 → 행정간호사 대시보드에는 항상 전체 통계 + 직접 입력한 개선 조치 기록 표시 → 신규간호사에게는 대화 도중 관련된 것만 그때그때 보여준다: 마찰 지도 답변 직후 → 마찰 지도 통계, Stay Menu 답변 직후 → 같은 기수의 Stay Menu 통계 + Stay Point, 대화 종료 시 → 행정 개선 조치 + 선배 한마디 + 응원의 한마디(참여 인정 메시지)

## 3. 기술 선택

**기본: Next.js** — 화면과 서버 코드(API)를 한 프로젝트에서 처리하고, Part 5에서 Vercel로 그대로 배포한다.

Next.js만으로는 안 되는 부분(데이터 저장, 로그인, AI 대화, 엑셀 읽기)에 아래 3가지를 추가로 제안한다. `.env`에 이미 준비된 토큰과도 맞아떨어진다. (위급 알림은 문자 없이 앱 내 알림만 쓰기로 해서, 별도 문자 발송 서비스는 필요 없다.)

| 추가 기술 | 쉬운 설명 |
|---|---|
| **Supabase** | 명단·응답·통계를 저장하는 데이터베이스이자, 행정간호사 로그인·비밀번호 재설정(이메일)까지 한 번에 해결해주는 서비스 — `.env`에 이미 접속 정보(`SUPABASE_ACCESS_TOKEN`)가 있다 |
| **OpenAI API** | 신규간호사와 대화하고, 위급 신호인지 스스로 판단하는 AI 두뇌 역할 — `.env`에 이미 접속 정보(`OPENAI_API_KEY`)가 있다 |
| **엑셀 읽기 라이브러리 (xlsx)** | 업로드한 엑셀 파일을 열어서 이름/전화번호/입사일 같은 표 내용을 프로그램이 읽을 수 있는 데이터로 바꿔주는 도구 |
